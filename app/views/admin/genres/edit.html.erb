<h2>
  ジャンル編集
</h2>

<%= form_with model: @genre, url: "/admin/genres/#{@genre.id}", local: true do |f| %>
  <%= f.label :ジャンル名 %>
  <%= f.text_field :name, autofocus: true %>
  <%= f.submit "変更を保存" %>
<% end %>
